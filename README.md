# 🎓 StudentData — CRUD Application in C# WinForms

**StudentData** is a modern Windows Forms application built with **.NET 8** and **Entity Framework Core** to manage student records efficiently.  
It provides full **CRUD operations (Create, Read, Update, Delete)** with a clean and interactive **DataGridView UI**.

---

## 🧰 Tech Stack

- **C# (.NET 8)**
- **Windows Forms (WinUI design inspired)**
- **Entity Framework Core 8**
- **SQL Server LocalDB** (can be changed to SQLite)
- **NuGet Packages:**
  - `Microsoft.EntityFrameworkCore`
  - `Microsoft.EntityFrameworkCore.SqlServer`
  - `Microsoft.EntityFrameworkCore.Design`
  - `Microsoft.EntityFrameworkCore.Tools`

---

## 🚀 Features

✅ Add, view, update, and delete students  
✅ Search bar with live filtering  
✅ DataGridView with modern colors and auto-refresh  
✅ Record count display  
✅ CSV export for backups  
✅ Hover animations and color transitions  
✅ Minimize to tray (still runs in background)  
✅ Clock and modern header panel  

---

